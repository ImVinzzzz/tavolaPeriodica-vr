import { useEffect, useRef } from 'react';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faXmark } from '@fortawesome/free-solid-svg-icons';
import type { ElementData } from '../../types/element';
import { ElementDetailCard } from './ElementDetailCard';
export function ElementModal({element,onClose}:{element:ElementData;onClose:()=>void}) { const closeRef=useRef<HTMLButtonElement>(null); useEffect(()=>{closeRef.current?.focus(); const fn=(e:KeyboardEvent)=>{if(e.key==='Escape')onClose();};window.addEventListener('keydown',fn);return()=>window.removeEventListener('keydown',fn)},[onClose]); return <div className="fixed inset-0 z-50 flex items-center justify-center p-3 sm:p-6" role="dialog" aria-modal="true" aria-label={`Dettagli di ${element.name}`}><button aria-label="Chiudi scheda" className="absolute inset-0 cursor-default bg-slate-950/70 backdrop-blur-md" onClick={onClose}/><div className="relative max-h-[94vh] w-full max-w-4xl overflow-y-auto [perspective:1400px]"><div className="animate-flip-in relative"><button ref={closeRef} type="button" onClick={onClose} aria-label="Chiudi" className="absolute right-3 top-3 z-10 flex h-9 w-9 items-center justify-center rounded-full border border-white/10 bg-slate-900/80 text-slate-300 transition hover:text-white focus:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200/70"><FontAwesomeIcon icon={faXmark}/></button><ElementDetailCard element={element}/></div></div></div>; }
